matrix:
  include:
    - language: php
      php: '7.1'
      before_install:
      - composer install --no-interaction
    - language: node_js
      node_js: '9'
      before_install:
      - yarn global add grunt-cli

